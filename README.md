# HireSight
A containerized ETL pipeline that scrapes job listings from Adzuna API and SerpAPI (Google Jobs), stores them in a PostgreSQL 16 database, and connects to Tableau for interactive dashboards
